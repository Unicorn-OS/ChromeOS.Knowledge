# Mounting a passed through USB Storage Device Fails!
```
try(){
mkdir try
sudo mount /dev/sda try
}
```

Result:
>mount: /home/me/try: permissioun denied
>dmesg(1) may have more information after failed mount system call.

